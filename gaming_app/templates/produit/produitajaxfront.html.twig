<div class="latest-products">
    <div class="container">
        <div class="row">

            {% for produit in produits %}
                <div class="col-md-4">
                    <div class="product-item">
                        <a href="{{ path('produit_show2',{'id': produit.id}) }}">
                        <img  src="/uploads/photos/{{produit.Image}}"   alt=""></a>
                        <div class="down-content">
                            <a href="package-details.html"><h4>{{ produit.Nom }}</h4></a>

                            <h6>{{ produit.Prix }} £</h6>

                            <p>Description Produit</p>

                            <small>

                                <strong title="Nights"><i class="fa fa-cube"></i>{{ produit.Quantite }}<a> Encore Disponible </a></strong> &nbsp;&nbsp;&nbsp;&nbsp;

                            </small>
                            <div class="col-lg-12">
                                <fieldset>
                                    <a href="{{ path('cart_add',{'id':produit.id}) }}" class="filled-button" style="margin: 5px;">Ajouter panier</a>
                                    <a href="{{ path('commande_new', {'idproduit':produit.id}) }}" class="filled-button" style="margin: 5px;">Commander</a>
                                </fieldset>
                            </div>
                        </div>
                    </div>
                </div>

            {% endfor %}

        </div>
    </div>
</div>
